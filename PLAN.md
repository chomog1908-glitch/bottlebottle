# 물병 정렬 퍼즐 (가칭: BottleBottle) — 개발 계획서

작성일: 2026-08-14

---

## 0. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 무엇 | 물병 색 정렬 퍼즐 게임 (Magic Sort! 류) |
| 왜 | 원작이 과금 없이는 고층 진행이 막혀서, 그 제약이 없는 버전을 직접 만든다 |
| 대상 사용자 | 어머니 (단일 사용자) |
| 배포 | APK 파일 직접 전달. 스토어 등록·상업화 없음 |
| 플랫폼 | Android (Flutter) |
| 절대 넣지 않는 것 | 광고, 결제, 하트/에너지, 로그인, 네트워크 통신, 광고 시청 보상 |

### 설계 원칙
1. **막히지 않는다** — 힌트·되돌리기·재시작 전부 무제한. 어떤 이유로도 진행이 잠기지 않는다.
2. **항상 풀 수 있다** — 생성된 모든 레벨은 해답이 존재함을 코드로 보장한다.
3. **오프라인 완결** — 인터넷 없이 100% 동작. 권한 요청 없음.
4. **읽기 쉽게** — 큰 터치 영역, 큰 글씨, 색약 대비 옵션. 어머니가 쓰실 것을 전제로 한다.

---

## 1. 게임 규칙 (확정)

- 화면에 여러 개의 병이 있고, 각 병은 최대 4칸의 색 액체를 담는다.
- 병 A를 탭 → 맨 위 색 덩어리가 들림. 병 B를 탭 → 부어짐.
- **부을 수 있는 조건**: B가 비어 있거나, B의 맨 위 색이 들고 있는 색과 같고, B에 빈 칸이 남아 있을 것.
- 같은 색이 연속으로 쌓여 있으면 한 번에 통째로 옮겨진다 (단, B의 남은 칸만큼만).
- **승리 조건**: 모든 병이 비어 있거나, 한 가지 색으로 가득 차 있을 것.
- 들고 있는 상태에서 같은 병을 다시 탭하면 취소.

---

## 2. 난이도 곡선

난이도를 키우는 축이 **셋**이다.

1. **색 가짓수** — 병 개수가 함께 늘어난다 (병 수 = 색 수 + 빈 병 수)
2. **병 깊이** — 한 병에 담기는 칸 수. 깊을수록 아래쪽 색을 꺼내기 어렵다
3. **빈 병 수** — 줄어들수록 숨 쉴 곳이 없어진다

| 레벨 구간 | 색 | 깊이 | 빈 병 | 병 수 | 체감 |
|---|---|---|---|---|---|
| 1–5 | 4 | 4칸 | 2 | 6 | 연습 |
| 6–20 | 5–6 | 4칸 | 2 | 7–8 | 연습 |
| 21–55 | 7–9 | 4칸 | 2 | 9–11 | 쉬움 |
| 56–100 | 10–12 | 4칸 | 2 | 12–14 | 보통 |
| 101–200 | 10–14 | **5칸** | 2 | 12–16 | 어려움 |
| 201–310 | 12–15 | **6칸** | 2 | 14–17 | 매우 어려움 |
| 311–420 | 13–15 | **7칸** | 2 | 15–17 | 매우 어려움 |
| 421–500 | 14–15 | **8칸** | 2 | 16–17 | 최고 난이도 |
| 501–700 | 15 | 8칸 | **1** | 16 | 최고 난이도 유지 |

**색은 15가지에서 멈춘다.** 색을 스무 가지씩 쓰면 퍼즐이 아니라 시력 검사가 된다.
그 뒤로는 **병을 깊게** 해서 난이도를 올린다. 깊은 병은 색을 헷갈리게 만들지 않으면서도
훨씬 멀리 내다봐야 풀린다.

**깊이가 한 칸 늘어나는 구간에서는 색 수를 잠시 줄인다.** 두 축을 동시에 올리면
난이도가 계단이 아니라 절벽이 된다. (테스트가 이 규칙을 강제한다.)

### 701부터 — 규칙으로 올린다

색과 깊이는 700에서 천장에 닿았다. 색은 15가지에서 멈추기로 했고 깊이 8칸이 화면의
한계다. 그 위로 난이도를 올리려면 **판을 키우는 대신 규칙을 바꿔야 한다.**

| 레벨 구간 | 규칙 | 색 | 깊이 | 격자 | 막히는 짝 |
|---|---|---|---|---|---|
| 701–850 | 가로±2 세로±2 | 13 | 6칸 | 5 | 26% |
| 851–1000 | 가로±2 **세로±1** | 10 | 5칸 | 4 | 35% |
| 1001–1150 | 가로±2 세로±1 | 10 | 5칸 | 3 | 41% |
| 1151–1300 | 가로±1 세로±1 | 10 | 4칸 | 4 | 56% |
| 1301–1600 | **트릭 A** | 8→11 | 5→6칸 | — |
| 1601–1900 | 트릭 **A+B** | 8→11 | 5→6칸 | — |
| 1901–2200 | 트릭 **A+C** | 8→11 | 5→6칸 | — |
| 2201–2800 | 이웃 제한 + 트릭 | 10 | 5칸 | 5 |

**이웃 제한** — 멀리 있는 병에는 붓지 못한다. 어디에 무엇을 두는지가 중요해진다.

**규칙을 적어 놓는 것과 규칙이 실제로 막는 것은 다르다.** 처음에는 ±3부터
시작했는데, 병 16개를 4×4로 놓으면 ±3은 어느 병에서든 모든 병에 닿는다.
막히는 짝이 0개 — 이름만 있고 아무 일도 하지 않는 규칙이었다. 그래서 구간마다
**실제로 막히는 짝의 비율**을 재서 26% → 35% → 41% → 56%로 오르게 다시 잡았다.
시험이 이 비율이 거꾸로 가지 않는지 지킨다.

**격자는 병 수를 나누어떨어지게 잡는다.** 병 16개를 한 줄 5개로 놓으면 마지막
줄에 한 개만 남는다. 규칙은 그 격자로 거리를 재므로, 외톨이 병은 눈에 보이는
자리와 규칙이 세는 자리가 어긋나 **바로 옆인데 못 붓는** 일이 생긴다.

**트릭 A** — 빈 병에 한 번 담으면 그 병은 그 색 전용이 된다. 비워도 풀리지 않는다.
**트릭 B** — 한 색만 남은 병도 그 색 전용. 완전히 비우면 풀린다.
**트릭 C** — 빈 병에서 시작한 병은 가득 찰 때까지 물을 도로 뺄 수 없다.

**새 규칙이 들어올 때 판을 줄였다가 다시 키운다.** 규칙과 크기를 한꺼번에 올리면
난이도가 계단이 아니라 절벽이 된다. 깊이가 늘 때 색을 줄이던 것과 같은 이유다.
(그래서 700까지 지키던 "깊이는 얕아지지 않는다"는 여기서 성립하지 않는다.
시험도 그 불변식을 700까지로 좁혔다.)

**이웃 제한 구간에서는 판을 크게 줄이지 않는다.** 이 규칙의 손잡이는 병 수나 깊이가
아니라 **격자의 한 줄 병 수**이기 때문이다. 아래 5.3에 실측을 적었다.

- 레벨은 **2800까지 확인**. 그 뒤로도 마지막 구간이 이어진다.
- 원작과 달리 **어떤 레벨에서도 막히지 않는다** — 힌트를 누르면 항상 다음 수를 알려준다.

---

## 3. 기술 스택

| 영역 | 선택 | 이유 |
|---|---|---|
| 프레임워크 | Flutter (stable) | 네이티브 APK, 애니메이션 강점 |
| 언어 | Dart | |
| 상태 관리 | `ChangeNotifier` + `ListenableBuilder` (기본 제공) | 외부 패키지 없이 충분한 규모 |
| 그래픽 | `CustomPainter` (병·액체 직접 그리기) | 이미지 에셋 불필요, 어떤 해상도에서도 선명 |
| 저장 | `shared_preferences` | 진행도·설정 로컬 저장 |
| 사운드 | `audioplayers` | 짧은 효과음 재생 |
| 햅틱 | `HapticFeedback` (Flutter 내장) | |

의존 패키지를 3개 이내로 유지한다 — 나중에 빌드가 깨질 위험을 줄이기 위함.

---

## 4. 아키텍처

```
lib/
├── main.dart                  앱 진입점, 테마 설정
├── model/
│   ├── bottle.dart            병 1개의 상태 (색 리스트)
│   ├── game_state.dart        보드 전체 상태, 수 두기/되돌리기/승리 판정
│   └── move.dart              한 수의 표현 (from, to, 개수)
├── logic/
│   ├── generator.dart         레벨 생성 (역방향 셔플)
│   ├── solver.dart            해답 탐색 (힌트 + 생성 검증용)
│   └── level_config.dart      레벨 번호 → 난이도 파라미터
├── ui/
│   ├── screens/
│   │   ├── title_screen.dart      타이틀 / 이어하기
│   │   ├── game_screen.dart       메인 게임 화면
│   │   ├── settings_screen.dart   설정
│   │   └── stats_screen.dart      통계
│   ├── widgets/
│   │   ├── bottle_widget.dart     병 1개 렌더링 + 탭 처리
│   │   ├── liquid_painter.dart    액체 CustomPainter
│   │   ├── pour_animation.dart    붓기 애니메이션
│   │   └── win_overlay.dart       클리어 연출
│   └── theme/
│       ├── palette.dart           색 팔레트 (일반 / 색약 모드)
│       └── app_theme.dart         라이트·다크 테마
└── services/
    ├── storage.dart           진행도 저장·불러오기
    └── audio.dart             효과음 관리
```

**핵심: 게임 로직(`model/`, `logic/`)은 Flutter에 전혀 의존하지 않는 순수 Dart로 작성.**
→ UI 없이 단위 테스트로 검증 가능. 버그를 화면에서 눈으로 찾을 필요가 없다.

---

## 5. 두 가지 핵심 알고리즘

### 5.1 레벨 생성 — 역방향 셔플 *(구현 완료)*
완성된 상태(각 병이 한 색으로 가득)에서 시작해, **한 수를 거꾸로 되감는 연산**을 반복해 섞는다.

되감기 한 번 = *"직전에 s에서 d로 k칸을 부었다고 치고 원상복구한다"* → d의 맨 위 k칸을 s로 옮긴다.

**구현 중 알게 된 함정** — 처음엔 "완성 상태에서 앞으로 두는 수를 무작위로 둬서 섞자"고 접근했는데,
완성된 상태에서 둘 수 있는 수는 *가득 찬 병을 통째로 빈 병에 옮기기*뿐이라 병의 자리만 바뀌고 색이 영영 안 섞였다.
앞으로 두는 수와 되감는 수는 다른 연산이라는 걸 놓친 것. **되감기는 "임의의 k칸"을 옮길 수 있어야 한다.**

되감기가 정확히 한 수로 복원되려면 두 조건이 필요하다 (둘 다 코드에 명시):
1. 받는 병 s는 비었거나 맨 위 색이 c와 **달라야** 한다. (같으면 되돌릴 때 k칸보다 많이 들려 원위치 불가)
2. k칸을 뗀 d는 색 c가 남아 있거나 **완전히 비어야** 한다. (아니면 되돌릴 때 물을 못 받는다)

이 규칙만 지키면 **풀 수 있음이 구조적으로 보장**되고, 채택 전 solver로 한 번 더 확인한다.

- 레벨 번호를 시드로 사용 → 같은 레벨은 항상 같은 판. (재시작해도 동일한 문제)
- 난이도 검증은 **해답 길이가 아니라 색 조각 수**로 한다. 탐색기가 최단 해답을 보장하지 않으므로
  해답 길이는 판의 어려움이 아니라 탐색기의 운을 재게 된다. 조각 수는 판 자체의 성질이다.
- 실측: 레벨당 생성 시간 **0~14ms**, 대부분 1~3회 시도 내 채택.

### 5.2 힌트 — 탐색기(solver) *(구현 완료)*
- **DFS + 방문 상태 정규화 + 수 정렬 휴리스틱.** 계획 초안의 BFS는 색 12개 규모에서
  메모리가 감당되지 않아 포기했다. 힌트는 "정답으로 가는 한 수"면 충분하고 최단성은 필요 없다.
- 상태 정규화: 병 순서는 규칙상 의미가 없으므로 정렬해 같은 상태로 취급 → 탐색 공간이 크게 준다.
- 수 정렬 휴리스틱(병을 완성시키는 수 > 병을 비우는 수 > 빈 병을 안 쓰는 수) 하나로 탐색량이 수십 배 줄었다.
- **"풀 수 없음"과 "예산 초과라 모르겠음"을 구분**한다. 이 둘을 뭉뚱그리면 풀 수 있는 레벨을 버리게 된다.
- 실측 성능이 충분해 아직 isolate가 필요 없다. 체감이 느려지면 `compute()`로 옮긴다.
- 힌트 버튼 → "이 병에서 저 병으로" 두 병을 반짝이게 표시. 자동으로 두지는 않는다(직접 두는 재미 보존).

---

### 5.3 규칙 엔진 *(구현 완료)*

규칙을 `RuleSet`이라는 **값**으로 만들어 `GameState`가 들고 다닌다.
레벨 번호를 묻는 조건문을 코드 곳곳에 심는 대신, 규칙을 아는 곳을 한 곳으로 모았다.
생성기·탐색기·화면은 그저 물어보기만 한다. 기본값은 레벨 1~700의 규칙과 정확히 같다.

**되감기를 앞으로 검증한다.** 되감기는 *"누군가 s에서 d로 k칸 부었다"*고 가정하고
그걸 되돌린다. 그 가정이 규칙상 불가능하면 **되돌아갈 길이 없는 판** — 곧 풀 수 없는
판이 만들어진다. 기존 조건 ①②는 기본 규칙에서만 이를 보장했다.
그래서 되감은 판을 실제로 만들어 `canPour`를 통과할 때만 채택한다.
규칙을 생성기에 다시 적지 않고 **`GameState`에게 물어본다** — 규칙이 늘어나도 생성기는
고칠 것이 없고, 두 곳의 해석이 어긋날 일도 없다.

**탐색 키에서 병 순서를 정렬하지 않는다.** 기본 규칙에서는 병의 자리가 의미 없어
정렬로 탐색 공간을 줄였다. 이웃 제한이 붙으면 자리가 의미를 가지므로, 정렬하면
서로 다른 상태를 같다고 잘못 판단해 **없는 해답을 있다고 하거나 그 반대가 된다.**
정규화를 잃은 손해는 가지 수가 줄어드는 이득이 메우고도 남았다(실측).

#### 실측으로 알게 된 것

**이웃 제한의 손잡이는 격자의 한 줄 병 수다.** 판 크기가 아니었다.
한 줄이 넓으면 같은 ±N이라도 병이 서로 멀어져 고립된다.

| 규칙 | 격자 | 판 | 결과 |
|---|---|---|---|
| ±3 | 8칸 | 색15 깊8 | **생성 실패** |
| ±3 | 5칸 | 색15 깊8 | 조각 60 · 해답 62수 |
| ±1 | 8칸 | 색10 깊5 | **생성 실패** |
| ±1 | 4칸 | 색10 깊5 | 성공 |

±3에 격자 5칸이면 색15 깊이8 그대로도 조각 60이 나온다. 기본 규칙 최고 난이도가
조각 46이니 **오히려 더 어렵다.** 판을 줄이지 않고 규칙만 조일 수 있다는 뜻이다.

**표를 적는 것과 그 표대로 판이 만들어지는 것은 다르다.** 처음 적은 표는
1001–1150 구간이 통째로 생성에 실패했고 701–850은 8판 중 2판이 싱거웠다.
표본 86개를 훑어 실패 0개가 될 때까지 고쳤다.

**느린 구간은 판을 한 단계 낮춰 해결한다.** 851–1000을 색14 깊이7로 두었더니
한 판에 최대 3.8초가 걸렸고 10판 중 2판은 아예 안 나왔다. 색13 깊이6으로 낮추니
평균 0.19초에 10판 전부 나온다. 지금 모든 구간이 최악 1.0초 안이고,
구간별 생성 시간을 시험으로 묶어 다시 느려지면 걸리게 했다.

**화면 배치가 규칙을 따라야 한다.** 지금까지 병 배치는 화면 크기가 정했다.
큰 폰은 한 줄에 8개, 작은 폰은 4개. 그대로 두면 **같은 레벨인데 폰마다 닿는 병이
달라진다.** 바로 옆에 보이는 병에 못 붓는 일이 생기고, 그건 누가 봐도 버그다.
그래서 규칙이 격자를 정한 판은 그 줄 수를 그대로 쓰고 크기만 화면에 맞춘다.

---

## 6. 꾸밈 요소 (합의된 범위)

### 시각
- **물 붓기 애니메이션** — 병이 기울고, 물줄기가 곡선을 그리며 떨어지고, 받는 병의 수위가 차오른다
- **액체 표면 흔들림** — 부은 직후 수면이 잠깐 출렁
- 병 유리 하이라이트·반사광 (CustomPainter로 그라디언트 처리)
- **클리어 연출** — 병들이 순서대로 살짝 튀어오르고 색종이(confetti) 파티클
- 레벨 전환 시 부드러운 페이드
- 잘못된 수를 시도하면 병이 좌우로 짧게 흔들림

### 청각 (짧고 조용하게)
- 병 집기 / **물 붓기(쪼르륵)** / 클리어 / 잘못된 수 — 효과음 4종
- 쪼르륵 소리는 붓는 양에 따라 길이가 달라진다 (1칸이면 짧게, 4칸이면 길게).
  물줄기 애니메이션과 시작·끝을 정확히 맞춘다 — 소리와 그림이 어긋나면 싸구려로 느껴진다.
- 설정에서 완전히 끌 수 있음. **BGM은 넣지 않음** (오래 하면 피로함)

### 테마
- 라이트 / 다크 / 시스템 따름
- 배경: 은은한 그라디언트 2~3종 선택
- **색약 모드**: 각 색 덩어리에 도형 기호(●▲■◆…)를 함께 표시.
  설정에서 켜는 **옵션이며 기본값은 꺼짐**

### 통계 화면
- 최고 도달 레벨, 총 클리어 수, 총 플레이 시간, 되돌리기·힌트 사용 횟수
- 자랑용일 뿐, 어떤 보상이나 압박도 연결하지 않는다

---

## 7. 편의 기능 (원작 불만 해소)

| 기능 | 동작 |
|---|---|
| 되돌리기 | 무제한. 한 수씩 계속 되감기 |
| 재시작 | 현재 레벨 처음부터 |
| 힌트 | 무제한. 다음 최적 수 표시 |
| 레벨 선택 | **난이도 → 병 구성 → 레벨**의 3단 폴더로 탐색. 어떤 폴더도 잠기지 않는다 |
| 건너뛰기 | 정 안 풀리면 다음 레벨로. 페널티 없음 |
| 자동 저장 | 앱을 꺼도 두던 판 그대로 복구 |
| 큰 글씨 모드 | UI 전체 확대 |

---

## 8. 개발 단계

### 0단계 — 환경 구축 ✅ 완료 (2026-08-15)

| 항목 | 버전 | 위치 |
|---|---|---|
| JDK | Temurin 17.0.20.8 | `C:\Program Files\Eclipse Adoptium\jdk-17.0.20.8-hotspot` |
| Flutter SDK | 3.47.0 stable (Dart 3.13.0) | `E:\dev\flutter` |
| Android SDK | Platform 36, Build-Tools 36.0.0, Platform-Tools 37.0.1 | `%LOCALAPPDATA%\Android\Sdk` |

- 사용자 환경변수 등록: `PATH`에 `E:\dev\flutter\bin`, `JAVA_HOME`, `ANDROID_HOME`
- Android SDK 라이선스 동의 완료
- `flutter doctor` — Flutter / Android 툴체인 / Chrome / 기기 전부 통과.
  Visual Studio 미설치 경고만 남으나 이는 **Windows 데스크톱 앱 빌드용**이라 본 프로젝트와 무관.

### 1단계 — 순수 로직 + 테스트 ✅ 완료 (2026-08-15)

구현된 파일 — 모두 Flutter 의존 없는 순수 Dart:

| 파일 | 내용 |
|---|---|
| `lib/model/move.dart` | 한 수의 표현 (from, to, count, color) |
| `lib/model/game_state.dart` | 보드 상태, 붓기 규칙, 되돌리기, 승리 판정, 정규화 키, 조각 수 |
| `lib/logic/level_config.dart` | 레벨 번호 → 난이도 파라미터 |
| `lib/logic/solver.dart` | 해답 탐색, 힌트 |
| `lib/logic/generator.dart` | 역방향 셔플 레벨 생성 |

**테스트 50개 전부 통과, `flutter analyze` 경고 0건.**
- 규칙 검증: 붓기 조건, 붓는 양(부분 이동 포함), 승리 판정, 되돌리기 정확 복원
- 탐색기: 해답 수순을 규칙 위에서 **실제로 재생해** 정말 이기는지 확인. 힌트를 끝까지 따라가면 이기는지도 확인
- 생성기: 레벨 1~100 + 고레벨 표본 전부 풀림, 색 개수 정확, 재현성, 헝클어짐 하한선

**이 단계에서 게임의 정확성은 끝났다.** 이후 UI 작업이 규칙을 깨뜨리면 테스트가 잡아낸다.

### 2단계 — 최소 플레이 가능 UI ✅ 완료 (2026-08-15)

| 파일 | 내용 |
|---|---|
| `lib/main.dart` | 앱 진입점, 라이트/다크 테마 |
| `lib/controller/game_controller.dart` | 화면과 로직을 잇는 `ChangeNotifier` |
| `lib/ui/screens/game_screen.dart` | 게임 화면 |
| `lib/ui/widgets/bottle_widget.dart` | 병 1개 (2단계에서는 사각형) |
| `lib/ui/theme/palette.dart` | 12색 팔레트 + 색약 기호 |

되는 것: 탭→탭으로 붓기, 들린 병 떠오르기, 되돌리기·힌트·다시·건너뛰기, 승리 배너, 레벨 이동, 색약 모드 토글.

**테스트 71개 전부 통과, `flutter analyze` 경고 0건.**
컨트롤러 테스트는 힌트만 눌러서 판을 끝까지 푸는 것까지 확인한다.

계획서 4장의 구조에 `lib/controller/`가 추가됐다. 선택 중인 병 같은 **화면에만 있는 상태**를
`GameState`에 섞으면 순수 로직이 오염되므로 따로 뒀다. 규칙 판단은 전부 `GameState`에 위임한다.

**구현 중 잡은 것** — 병 테두리가 상자 안쪽을 차지해 액체 4칸이 3픽셀 넘쳤다.
게다가 강조할 때 테두리를 굵게 하면 병 크기가 들썩였다. 굵기는 항상 고정하고 **색만** 바꾸도록 수정.

### 3단계 — 게임 화면 완성 ✅ 완료 (2026-08-15)

| 파일 | 내용 |
|---|---|
| `lib/ui/widgets/liquid_painter.dart` | 유리 병과 액체를 직접 그리는 `CustomPainter` |
| `lib/ui/widgets/board_layout.dart` | 화면 크기에 맞춰 병 크기·줄 수를 정하는 순수 함수 |
| `lib/services/storage.dart` | 진행도·설정 저장 |

- **유리 병** — 둥근 바닥, 위아래 그라디언트, 왼쪽 빛 반사. 칸 경계에 얇은 그늘을 넣어
  같은 색이 몇 칸인지 셀 수 있게 했다. 이게 없으면 같은 색 4칸이 한 덩어리로 보인다.
- **반응형 배치** — 병이 6개에서 17개까지, 깊이가 4칸에서 8칸까지 변하므로 크기를 고정할 수 없다.
  남은 공간에 맞춰 줄 수와 병 크기를 계산한다. 순수 함수로 떼어 놓아 화면 없이 시험할 수 있다.
- **자동 저장** — 수를 둘 때마다 저장. **레벨 번호와 둔 수 목록만** 남긴다.
  레벨 생성이 결정적이라 그 둘만 있으면 보드·되돌리기 기록·빈 병 기록이 통째로 복원된다.
  병 내용물을 저장하면 되돌리기 기록과 따로 놀아 복원 후 되돌리기가 깨진다.
- **빈 병 사용량 표시** (사용자 제안) — 판을 푸는 동안 **동시에 점유한 빈 병 수의 최대값**을 기록한다.
  단순히 "지금 몇 개 비었나"로는 안 된다. 색을 완성해 병을 비우면 빈 병이 처음보다 늘 수도 있기 때문.
  되돌리기를 하면 기록도 함께 되돌아간다.

**테스트 95개 전부 통과, `flutter analyze` 경고 0건.**
작은 폰~큰 폰 3종 화면 × 레벨 8구간에 대해 화면 넘침이 없는지 확인한다.

**구현 중 잡은 것** — 깊이를 8칸까지 늘리자 헝클어짐 기준(색 개수 × 2)이 헐거워져
깊이 8칸짜리 판이 27수 만에 끝났다. 기준을 **전체 칸 수에 비례**하도록 바꿔 해결.
빈 병이 1개인 최고 난이도 구간은 섞을 여지가 좁아 기준을 조금 낮춘다.

### 3.5단계 — 레벨 선택 폴더 ✅ 완료 (2026-08-15)

| 파일 | 내용 |
|---|---|
| `lib/logic/level_groups.dart` | 난이도(대분류) / 병 구성(중분류) 묶음 |
| `lib/ui/screens/difficulty_screen.dart` | 대분류 — 난이도 폴더 목록 |
| `lib/ui/screens/group_screen.dart` | 중분류 — "병 N개 · M층" 목록 |
| `lib/ui/screens/level_select_screen.dart` | 레벨 격자 |
| `lib/ui/widgets/folder_card.dart` | 두 폴더 화면이 함께 쓰는 카드 |

**난이도 → 병 개수·층수 → 레벨**의 3단 구조. 각 단계마다 `12 / 30 완성` 진행 막대를 보여준다.

묶음 경계를 손으로 적지 않고 **`LevelConfig`에서 읽어 만든다.** 난이도표를 고치면
화면이 저절로 따라오므로 둘이 어긋날 수 없다. (테스트가 이 일치를 검증한다.)

**어떤 폴더도 잠기지 않는다.** 레벨 1인 사람도 최고 난이도 폴더를 열 수 있다.
클리어 표시는 하되, 안 한 레벨에 X나 자물쇠를 두지 않는다 —
빈칸을 표시로 채우면 "못 한 것을 세는 화면"이 된다.

**구현 중 잡은 버그 (사용자 발견)** — 빈 병 2개를 준다고 해놓고 시작부터 하나가 차 있는 판이 나왔다.
되감기 셔플이 빈 병을 즐겨 채우는데 마지막 상태를 그대로 썼기 때문. 화면에도
0수인데 "빈 병 1/2 사용 중"으로 떴고, 빈 병 1개짜리 판에서 "2개를 모두 썼다"는 말까지 나왔다.
→ 섞는 **도중에** 조건을 만족하는 순간을 잡아 두었다가 그것을 판으로 쓰도록 고쳤다.
약속한 빈 병 개수는 완화 경로에서도 반드시 지킨다.

### 4단계 — 꾸밈 ✅ 완료 (2026-08-15)

| 파일 | 내용 |
|---|---|
| `lib/ui/widgets/pour_overlay.dart` | 병이 기울고 물줄기가 곡선으로 떨어지는 붓기 연출 |
| `lib/ui/widgets/confetti.dart` | 클리어 색종이 |
| `lib/ui/widgets/achievement_toast.dart` | 도전과제 달성 알림 |
| `lib/services/audio.dart` + `assets/audio/*.wav` | 효과음 4종 (집기·붓기·완성·잘못된 수) |
| `lib/controller/settings_controller.dart` | 테마·큰 글씨·소리·색약 모드 (앱 전체가 하나를 본다) |
| `lib/ui/screens/settings_screen.dart` | 설정 화면 |
| `tool/make_sounds.py` | 효과음을 코드로 합성 — 음원을 어디서도 받아오지 않는다 |

- **붓기 연출은 규칙을 늦추지 않는다.** 게임 상태는 이미 부어진 뒤이고, 화면이 그것을
  **되감아서** 보여준다. 규칙을 애니메이션에 맞춰 늦추면 규칙 코드가 화면 사정에 오염된다.
- 쪼르륵 소리는 붓는 양에 따라 길이가 달라지고, 물줄기 애니메이션과 시작·끝을 맞춘다.
- 잘못된 수를 시도하면 그 병이 좌우로 짧게 흔들린다. 같은 병을 연달아 잘못 눌러도
  매번 반응하도록 `rejectTick`을 따로 센다. (`rejected` 값만 보면 두 번째부터 안 바뀐다.)

**구현 중 잡은 것** — 병이 화면 위쪽에 몰리고 아래 절반이 비었다. `SingleChildScrollView`가
자식에게 높이를 **무한으로** 주기 때문에 그 안의 가운데 정렬이 아무 일도 하지 않았다.
최소 높이를 화면만큼 잡아 해결. 넘칠 때는 그보다 커지므로 스크롤은 그대로 된다.

### 4.5단계 — 도전과제 · 통계 · 홈 ✅ 완료 (2026-08-15)

계획서에 없던 단계다. 통계 화면을 만들다가 "기록이 있으면 도전과제도 되겠다"로 이어졌다.

| 파일 | 내용 |
|---|---|
| `lib/logic/achievements.dart` | 누적 기록(`PlayStats`)과 도전과제 29개 — **순수 Dart** |
| `lib/logic/stats_summary.dart` | 난이도별 진행·출석 계산 — **순수 Dart** |
| `lib/services/achievement_tracker.dart` | 게임 사건을 기록으로 옮기고 새로 얻은 것을 알려준다 |
| `lib/ui/screens/home_screen.dart` | 첫 화면 — 가장 큰 버튼은 언제나 "이어서 하기" |
| `lib/ui/screens/achievements_screen.dart` | 도전과제 대시보드 (고리·급별·거의 다 왔습니다·묶음 필터) |
| `lib/ui/screens/stats_screen.dart` | 통계 대시보드 (숫자 타일·고리·난이도별 막대·최근 2주 출석) |
| `lib/ui/widgets/stat_widgets.dart` | 대시보드 부품 (`StatTile`·`StatRing`·`StatBar`·`StatCard`) |

**설계에서 지킨 것**
- **어떤 도전과제도 보상을 주지 않는다.** 레벨을 열지도, 힌트를 주지도 않는다. 자랑용일 뿐이다.
- **"힌트를 쓰지 마라"는 식은 넣지 않았다.** 스스로 푼 판을 칭찬할 뿐, 힌트를 쓴 판을 깎지 않는다.
  통계 화면도 힌트·되돌리기 횟수를 부끄러운 숫자가 아니라 그냥 사실로 적는다.
- **얻었는지 여부는 저장하지 않는다.** `PlayStats` 하나만 있으면 29개가 전부 다시 계산된다.
  날짜만 따로 남긴다 — 그건 지나가면 알 길이 없어서다. 저장할 것이 적을수록 나중에 깨질 것도 적다.
- 컨트롤러는 기록을 세지 않는다. `GameEvent`로 **무슨 일이 있었는지 알리기만** 하고,
  어떻게 쌓을지는 듣는 쪽이 정한다. 게임 규칙과 기록을 섞지 않기 위함이다.

**구현 중 잡은 것 둘**
1. **복원은 기록에 넣지 않는다.** 저장된 수순을 재생할 때 사건이 나가면 앱을 켤 때마다
   둔 수가 다시 세어진다. 그래서 `restore()`가 끝난 **뒤에** 사건을 듣기 시작한다.
2. **불러오기 전에는 아무것도 세지 않는다.** 빈 기록 위에 덮어써서 지난 기록을 날리는 것이
   최악이라, 추적기가 `load()` 전이면 조용히 무시한다. 테스트가 이 둘을 지킨다.

### 4.6단계 — 레벨 확장과 규칙 엔진 ✅ 완료 (2026-09-07)

- [x] **규칙을 값으로** — `RuleSet`을 `GameState`가 들고 다닌다. 기본값은 1~700과 동일
- [x] **생성기가 규칙을 지킨다** — 되감기를 앞으로 검증해, 되돌아갈 길이 없는 판을 막는다
- [x] **난이도표 701~2800** — 이웃 제한 4단계 + 트릭 A/B/C. 표본 86개 생성 실패 0개
- [x] **화면** — 닿지 않는 병 흐리게, 잠긴 병 테두리 색, 안내 카드 + ⓘ 단추
- [x] **화면 배치가 규칙을 따른다** — 폰 크기가 격자를 정하면 폰마다 규칙이 달라진다

자세한 근거는 5.3에 적었다.

### 4.7단계 — 저장이 판 변화를 견디게 ✅ 완료 (2026-09-07)

**어머니 폰의 실제 기록을 받아 넣어 보고 발견했다.** 레벨 431의 저장된 43수가
첫 수부터 재생되지 않았다. 레벨 432를 고치면서 셔플이 난수를 한 번 더 뽑게 되었고,
그 한 번이 난수 흐름을 밀어 **레벨 1~700 중 699개의 판이 바뀌었다.**

진행도·도전과제·통계는 레벨 번호로 남으므로 전부 무사했다. 잃은 것은 **두고 계시던
판 하나**뿐이다. 이미 푸신 427판을 다시 푸실 필요는 없다.

- [x] **시작 보드를 함께 저장** — 판을 다시 만들지 않아도 되므로 생성기가 바뀌어도
      두시던 판이 살아남는다. 지금 보드가 아니라 **시작 보드**를 담아, 수순을 그 위에
      다시 두는 방식을 유지했다. 그래야 되돌리기 기록이 함께 살아난다
- [x] **예전 저장도 그대로 읽힌다** — 보드가 없으면 예전처럼 레벨 번호로 만든다.
      어머니 폰에 이미 들어 있는 저장이 이 경우다
- [x] **손상된 보드는 무시** — 규격에 안 맞으면 조용히 생성된 판으로 연다
- [x] **실제 기록으로 시험** — 지어낸 자료가 아니라 어머니 폰에서 뽑은 진짜 기록을
      시험에 넣었다. 형식을 고쳤을 때 이미 쌓인 기록이 깨지는지는 이걸로만 알 수 있다

> 💡 **교훈** — "생성기를 안 고치겠다"는 약속으로는 이걸 막을 수 없다.
> 결정적 생성에 기대어 저장을 줄인 것은 영리했지만, **그 결정성을 내가 깰 수 있다는
> 것**을 계산에 넣지 않았다. 몇 KB를 아끼려다 판 하나를 잃었다.

### 5단계 — 마무리 ✅ 완료 (2026-09-07)

- [x] **릴리스 서명** — `keystore/bottlebottle-release.jks` (PKCS12, 유효기간 100년) +
      `android/key.properties`. 둘 다 `.gitignore`에 넣었다.
      **이 두 파일은 잃어버리면 복구할 수 없다.** 다른 열쇠로 서명한 APK는 기존 앱 위에
      덮어 설치되지 않아, 지우고 다시 깔아야 하고 그러면 진행도가 통째로 사라진다.
- [x] **릴리스 APK 빌드** — 49.5MB. 서명 검증 완료(`CN=BottleBottle`, 디버그 키 아님)
- [x] **실기기 테스트** — 갤럭시 S21(Android 15), 갤럭시 A54(Android 14, 어머니 폰).
      무선 디버깅(`adb pair` → `adb install -r`)으로 설치
- [x] **기록 내보내기·불러오기** — 설정 화면. 진행도·도전과제·설정 전부를 글자 하나로 뽑아
      클립보드에 넣는다. 폰을 바꾸실 때 옮길 유일한 수단이다. 의존성은 늘지 않았다
- [x] **앱 아이콘** — 병 세 개 그림. 적응형 아이콘(배경 + 앞 그림)으로 넣어,
      기기가 제 모양대로 깎아 쓰고 누를 때 두 층이 어긋나며 움직인다.
      통짜로 넣으면 이미 둥근 모서리가 한 번 더 깎여 어색해진다.
      병은 가운데 안전 영역(108dp 중 72dp) 안에 앉혔다
- [x] **스플래시 화면** — 켤 때 흰 화면이 번쩍이던 것. 기본값이 흰색이라 어두운
      테마에서는 매번 눈이 부셨다. 아이콘 배경색을 깔고 병을 가운데 놓아
      아이콘에서 앱으로 이어지게 했다. 어두운 테마에는 어두운 바탕을 쓴다
- [x] **설치 안내문** — `설치안내.html`. 어머니가 폰에서 읽으실 한 장짜리.
      큰 글씨, 네 단계, "하시던 게임은 그대로 있습니다"를 따로 뺐다.
      **"기존 앱을 지우고 설치"를 누르지 마시라**는 경고가 핵심이다 — 그걸
      누르면 기록이 진짜로 날아간다
- [ ] 앱 이름 — `bottlebottle` 그대로 두기로 했다 (2026-09-07, 사용자 결정)

**업데이트해도 데이터가 유지되는 조건** — 같은 `applicationId`(`com.bottlebottle.bottlebottle`) +
**같은 서명키**로 빌드한 APK를 `adb install -r`로 덮어 설치. 날아가는 경우는 넷뿐이다:
①삭제 후 재설치 ②설정에서 앱 데이터 삭제 ③`applicationId` 변경 ④서명키 변경.

> ⚠️ **`flutter install`을 쓰면 안 된다.** 이 명령은 기존 앱을 **지우고** 깐다.
> 실제로 그렇게 해서 실기기의 진행도를 한 번 날렸다. `adb install -r`을 쓸 것.

**빌드 환경 메모** — 윈도우에서 코틀린 증분 컴파일 캐시를 닫지 못해 릴리스 빌드가 실패한다.
`android/gradle.properties`에 `kotlin.incremental=false`를 넣어 해결했다. 빌드가 조금 느려지지만
(약 60초) 릴리스를 자주 굽는 프로젝트가 아니다.

### 6단계 — 어머니 피드백 반영 🚧 진행 중
실제로 써보신 뒤 나오는 요청 반영. (글씨 크기, 색, 난이도 등)

- [x] **레벨 목록이 700까지만 보이던 것** — 2800레벨을 만들어 놓고 목록에서
      감췄다. 어머니가 431에서 목록을 여셨을 때 2100레벨이 통째로 숨어 있었다

---

## 9. 미결정 사항 (진행하며 정할 것)

- [ ] 앱 이름과 아이콘 — 어머니가 좋아하실 만한 것으로. **지금 남은 가장 큰 숙제다**
      (홈 화면에서 기본 Flutter 아이콘과 `bottlebottle`이라는 이름으로 보인다)
- [ ] 마스코트 캐릭터를 넣을지 (원작엔 다람쥐가 있음). 넣는다면 그림을 어떻게 조달할지
- [x] 레벨당 별점·목표 수 같은 평가 요소 — **넣지 않는다.** 대신 도전과제를 넣되
      보상도 압박도 연결하지 않았다. 빈 병 사용량도 잘잘못을 가르지 않고 사실만 적는다
- [x] 어머니 폰 — **갤럭시 A54 (SM-A546S), Android 14.** minSdk 24(Android 7)이므로 여유 있다

---

## 10. 다음 행동

1. **어머니 피드백** — v0.2.0을 실제로 써보신 뒤 나오는 요청 반영. 남은 일은 이것뿐이다
2. 앱 이름은 `bottlebottle` 그대로 둔다. 바꾸고 싶어지시면 그때 바꾼다

**무선 디버깅 메모** — `adb pair`가 계속 실패했다. 원인은 둘이었다.
① PC의 Wi-Fi가 "공용 네트워크"로 잡혀 있어 윈도우가 TCP를 막았다(핑은 통과하므로
헷갈린다). "개인 네트워크"로 바꾸면 풀린다. ② 폰 IP가 바뀌어 있었다.
`adb mdns services`로 폰이 실제로 어디 있는지 먼저 확인할 것.
결국 APK 파일을 직접 옮기는 쪽이 빨랐다.

**어머니 현황 (2026-09-07 기준)** — 레벨 431 진행 중, 427판 클리어, 19일 플레이,
13,951수. 되돌리기 1,119회 · 힌트 200회로 **되돌리기를 주로 쓰신다.** 새 규칙에서
되돌리기가 불안해지면 안 되는 이유다. 빈 병을 하나도 안 쓰고 푼 판은 아직 0판이므로,
빈 병을 잠그는 트릭 구간(1301~)은 체감 난이도가 크게 오를 수 있다.
새 내용까지는 아직 270레벨쯤 남았다.

---

## 11. 지금까지의 규모 (2026-09-07)

| 항목 | 값 |
|---|---|
| 레벨 | **1~2800** (전 구간 생성 확인, 최악 1.0초) |
| 테스트 | **236개 전부 통과** |
| `flutter analyze` | **경고 0건** |
| 의존 패키지 | `shared_preferences`, `audioplayers` — **2개** (3개 이내 원칙 유지) |
| 화면 | 홈 · 게임 · 난이도 · 병 구성 · 레벨 선택 · 도전과제 · 통계 · 설정 (8개) |
| 도전과제 | 29개 (여정 10 · 깊은 병 6 · 아껴 쓰기 4 · 스스로 5 · 꾸준함 4) |
| 규칙 | 기본 + 이웃 제한 4단계 + 트릭 3종 |
| 릴리스 APK | 50.1MB (v0.2.1) |

**게임 로직(`model/`, `logic/`)은 여전히 Flutter를 전혀 모른다.** 도전과제와 통계 계산까지
순수 Dart로 넣었으므로, 화면 없이 시험되는 범위가 오히려 넓어졌다.
